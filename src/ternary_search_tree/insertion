#Ternary search tree 

class TenaryTreePointers:
    def __init__(self, a, b, c):
        self.left = a # Left: points to the node whose value is less than the value in the current node
        self.equal = b # Equal: points to the node whose value is equal to the value in the current node
        self.right = c # Right: points to the node whose value is greater than the value in the current node

class TernaryTreeInsertion: 
    def ___init___(self): 
        self.root = None # Starting the tree with no nodes 

    def insertMain(self,word): 
        # Add here the main function 
    
    def insertionAuxiliar(self,node,word): # self: gives reference to the tree itself; node: current node; work: new work to be add; 
